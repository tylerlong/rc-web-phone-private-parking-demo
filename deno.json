{
  "tasks": {
    "lint": "deno fmt && deno lint --fix",
    "serve": "deno run -A npm:vite",
    "reinstall": "rm -rf node_modules && deno install"
  },
  "compilerOptions": {
    "lib": [
      "DOM"
    ],
    "jsx": "react"
  }
}
